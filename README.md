# chatbot-telegram.
🤖 Chatbot no Telegram integrado via n8n que consulta a previsão do tempo em tempo real (OpenWeather API) e gera respostas personalizadas e humanizadas utilizando a IA do Google Gemini. Configurado para rodar localmente com Docker Compose e ngrok.
